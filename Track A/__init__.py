"""Genome Firewall — Module 1 (Track A): the Genome Reader.

The folder name "Track A" has a space, so it is used the same way the app uses
the other tracks — add the folder to sys.path, then import the modules directly:

    import sys; sys.path.insert(0, "Track A")
    from build_features import run_genome_reader, build_features_table
    from feature_annotator import get_annotator, load_spec

To add your own annotation tool or dataset, see "Track A/README.md".
"""
