"""Genome Firewall — Module 1 (Track A): the Genome Reader.

Now a proper package (no space in the name), so import normally:

    from module1_reader.build_features import run_genome_reader, build_features_table
    from module1_reader.feature_annotator import get_annotator, load_spec

To add your own annotation tool or dataset, see module1_reader/README.md.
"""
